# interpreter
An interpreter for the "monkey" language written in go
---
### example "monkey" language
<pre>
    let five = 5;
    let ten = 10;

    let add = fn(x, y) { 
        x + y;
    };  

    let result = add(five, ten);
</pre>